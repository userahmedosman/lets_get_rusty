[package]
name = "draw"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "1.0", feature = ["dervie"], optional=true}
rgb =  {version = "0.8.25", feature = ["serde"], optional=true}

[features]
default=["color"]
color = ["dep:rgb"]
shapes = ["color", "dep:serde", "rgb?/serde"]
